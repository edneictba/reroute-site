# Guards

Protecao de rotas do REROUTE Portal.

Na Sprint 2, `route-guard.js` protege paginas privadas, redireciona usuarios sem sessao para `/portal/login/` e impede que o dashboard seja exibido antes da sessao ser resolvida.
